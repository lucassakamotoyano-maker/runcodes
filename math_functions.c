#include <stdio.h>
#include <math.h>

int main()
{
    //int x =9;
    //float x = 2.71;
    //int x = -3;

    
    //x = sqrt(x);
    //x = pow(x, 2);
    //x = round(x);
    //x = ceil(x);
    //x = floor(x);
    //x = abs(x); //x has to be an integer!
    //x = log(x); //x has to be a float + it´s the natural log!
    //x = sin(x);
    //x = cos(x); //trigonometri  functions use float in radians
    //x = tan(x);
   
   
   
    //printf("%d", x);
    // printf("%f", x );


